const $=s=>document.querySelector(s), $$=s=>document.querySelectorAll(s);const nav=$('#nav');$('.menu-toggle').onclick=()=>nav.classList.toggle('open');$$('.services button').forEach(b=>b.onclick=()=>{$('#service').value=b.closest('article').dataset.service;$('#agendar').scrollIntoView({behavior:'smooth'})});const date=$('#date'),time=$('#time');const now=new Date(),today=new Date(now-now.getTimezoneOffset()*60000).toISOString().slice(0,10);date.min=today;date.value=today;['08:00','09:00','10:00','11:00','13:00','14:00','15:00','16:00','17:00','18:00'].forEach(t=>time.insertAdjacentHTML('beforeend',`<option>${t}</option>`));$('#bookingForm').onsubmit=e=>{e.preventDefault();const data={service:$('#service').value,date:date.value.split('-').reverse().join('/'),time:time.value,name:$('#name').value,phone:$('#phone').value};localStorage.setItem('ultimoAgendamento',JSON.stringify(data));$('#msg').textContent=`${data.name}, seu ${data.service.toLowerCase()} foi reservado para ${data.date} às ${data.time}. Em produção, a confirmação pode ser enviada automaticamente pelo WhatsApp ou e-mail.`;$('#modal').classList.add('show');e.target.reset();date.value=today};const close=()=>$('#modal').classList.remove('show');$('.close').onclick=close;$('#ok').onclick=close;$('#modal').onclick=e=>{if(e.target.id==='modal')close()};$('#phone').oninput=e=>{let v=e.target.value.replace(/\D/g,'').slice(0,11);if(v.length>10)v=v.replace(/^(\d{2})(\d{5})(\d{4}).*/,'($1) $2-$3');else if(v.length>6)v=v.replace(/^(\d{2})(\d{4})(\d{0,4}).*/,'($1) $2-$3');else if(v.length>2)v=v.replace(/^(\d{2})(\d{0,5}).*/,'($1) $2');e.target.value=v};
